package sheet_codes;

import java.util.Arrays;

public class plus_one {
    public static void main(String[] args) {
        // long c=0;
        // long a[]={1,9,9};
        // for(long i=0;i<a.length;i++){
        // if(a[i]==9){
        // c++;
        // }
        // }
        // if(c==0){
        // a[a.length-1]=a[a.length-1]+1;
        // System.out.prlongln(Arrays.toString(a));
        // }
        // else if(c==a.length){
        // long b[]=new long[a.length+1];
        // b[0]=a[0]-8;
        // System.out.prlongln(Arrays.toString(b));
        // }
        // else{
        // for(long i=0;i<a.length;i++){
        // if(i==0)
        // a[0]=a[0]+1;

        // else{
        // a[i]=0;
        // }}

        // }
        // // a[a.length-c]=a[a.length-c]+1;
        // System.out.prlongln(Arrays.toString(a));

        // -----------------------------------------------
        // long v[] = { 9,9 };
        // String s = "";
        // for (long i = 0; i < v.length; i++) {
        // s = s + v[i];

        // }
        // long b = longeger.parselong(s) + 1;
        // String st = longeger.toString(b);
        // long len = st.length();
        // long a[] = new long[len];
        // for (long i = 0; i < a.length; i++) {
        // a[i] = st.charAt(i) - '0';

        // }

        // System.out.prlongln(Arrays.toString(a));

        // -----------------------------------------------
        // long[] s = { 9,8,7,6,5,4,3,2,1,8 };
        // long f = 0;
        // long ten = (long) Math.pow(10, s.length - 1);
        // System.out.println(ten);
        // for (int i = 0; i < s.length; i++) {
        //     f = f + s[i] * ten;
        //     System.out.println(f);
        //     ten = ten / 10;
        //     // System.out.prlongln(ten);
        // }
        // f = f + 1;
        // System.out.println(f);
        // long d[] = new long[s.length];
        // int h = 0;
        // long r = (long) Math.pow(10, s.length - 1);
        // while (f != 0) {

        //     d[h++] = f / r;
        //     f=f%r;
        //     r = r / 10;
            
        // }

        // System.out.println(Arrays.toString(d));
        int f='9',g='0';
        System.out.println(f+" "+g);
    }
}
